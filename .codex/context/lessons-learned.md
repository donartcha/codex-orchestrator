# Lessons Learned

Primary lesson storage is MariaDB via `.codex/context-api`.

Mirror reusable lessons here when they help humans understand repeated failures or workflow constraints.

Use `open_context()` for persisted lessons.
