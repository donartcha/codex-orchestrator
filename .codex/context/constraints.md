# Constraints

- Use PowerShell as the primary shell.
- Use `.codex/context-api/codex_context/context.py` and `open_context()` for persistent memory.
- Do not store secrets in MariaDB or Markdown mirrors.
- Do not modify unrelated files.
- Do not execute destructive commands without confirmation.
