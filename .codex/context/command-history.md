# Command History

Primary command history storage is MariaDB via `.codex/context-api`.

Mirror only high-value command patterns here. Do not include secrets or full `.env` output.

Use `open_context()` for persisted command history.
