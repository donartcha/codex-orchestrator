# Task Breakdown

Primary task storage is MariaDB via `.codex/context-api`.

Use this file as a readable mirror for large task plans, not as the source of truth.

Use `open_context()` for persisted tasks.
