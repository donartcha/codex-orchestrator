# Project State

Human-readable mirror of current project state. MariaDB through `.codex/context-api` is the primary source.

- Persistent memory API: `from codex_context.context import open_context`
- Shell: PowerShell
- Context API status: available when `python codex_memory.py check` succeeds from `.codex/context-api`
