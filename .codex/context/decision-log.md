# Decision Log

Primary decision storage is MariaDB via `.codex/context-api`.

Mirror key decisions here only when useful for humans. Do not store secrets.

Use `open_context()` for persisted decisions.
