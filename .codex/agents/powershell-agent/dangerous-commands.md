# Dangerous Commands

Require confirmation before destructive commands, recursive deletion, force deletion, credential changes, real `.env` edits or writes outside the workspace. Record approved destructive command decisions through `open_context()` without storing secrets.
