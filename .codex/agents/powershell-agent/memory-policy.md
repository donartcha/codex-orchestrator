# Memory Policy

Use `.codex/context-api` and `open_context()` to retrieve command history and lessons before complex commands. Record relevant commands, errors, corrections and reusable prevention strategies.
