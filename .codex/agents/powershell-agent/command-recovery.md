# Command Recovery

When a command fails, read the complete error, consult command history and lessons, identify root cause, correct the command, retry only when safe and record both failed and corrected commands through `open_context()`.
