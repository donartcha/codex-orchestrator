# Severity Rules

- Critical: data loss, security exposure, destructive behavior.
- High: user-visible regression, broken workflow, failed core validation.
- Medium: maintainability risk with plausible impact.
- Low: minor clarity or follow-up improvement.
- Persist durable findings with `open_context()` from `.codex/context-api`.
