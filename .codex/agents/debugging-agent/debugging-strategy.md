# Debugging Strategy

- Reproduce before fixing when feasible.
- Minimize diagnostic changes.
- Keep evidence attached to each hypothesis.
- Stop retrying when a failure mode is understood and needs escalation.
- Use `.codex/context-api` through `open_context()` to compare prior failures.
