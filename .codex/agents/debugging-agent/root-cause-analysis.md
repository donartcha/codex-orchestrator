# Root Cause Analysis

- Capture exact symptom and error.
- Compare against command history and lessons.
- Form falsifiable hypotheses.
- Validate one hypothesis at a time.
- Record root cause and prevention strategy.
- Persist reusable findings with `open_context()` from `.codex/context-api`.
