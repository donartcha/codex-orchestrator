# Memory Policy

Use `open_context()` from `.codex/context-api` before, during and after implementation. Record technical decisions, validation commands, failures and follow-up tasks. Never store secrets.
