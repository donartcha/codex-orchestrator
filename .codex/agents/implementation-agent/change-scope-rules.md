# Change Scope Rules

- Modify only files needed for the assigned task.
- Avoid unrelated refactors and formatting churn.
- Do not introduce dependencies without approval.
- Work with existing user changes; never revert them without permission.
- Check persisted constraints through `open_context()` before broad edits.
