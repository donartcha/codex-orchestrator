# Test Strategy

- Detect the framework from project files.
- Run focused tests before broad suites.
- Prefer deterministic tests over brittle snapshots.
- Record commands and outcomes through `open_context()`.
