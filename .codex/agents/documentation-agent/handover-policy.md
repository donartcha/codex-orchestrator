# Handover Policy

- Include current status, files touched, validations, blockers and next steps.
- Reflect durable handover items through `.codex/context-api` and `open_context()`.
- Use `.codex/context/` only as a human-readable mirror.
