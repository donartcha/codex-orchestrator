# Documentation Rules

- Document workflows, constraints, decisions and commands that will be reused.
- Keep prose concise and actionable.
- Prefer links to source files over duplicated content.
- Do not document secrets.
- Use `.codex/context-api` through `open_context()` for durable memory.
