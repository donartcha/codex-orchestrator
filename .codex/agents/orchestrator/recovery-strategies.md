# Recovery Strategies

- On failed validation, assign debugging-agent or testing-agent based on the failure.
- On failed shell commands, assign powershell-agent.
- On context conflicts, assign context-manager.
- Record root cause, correction and reusable lesson through `open_context()`.
