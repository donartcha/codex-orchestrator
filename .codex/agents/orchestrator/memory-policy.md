# Memory Policy

Use `.codex/context-api` with:

```python
from codex_context.context import open_context
```

Record task breakdowns, decisions, risks and follow-ups. Never store secrets or irrelevant historical dumps.
