# Execution Policy

- Follow the mandatory workflow from `.codex/AGENTS.md`.
- Start with memory bootstrap.
- Decompose before editing when scope is complex.
- Validate and review before final memory update.
- Use `.codex/context-api` through `open_context()` for every memory step.
