# Context API Retrieval Policy

Use `.codex/context-api/codex_context/context.py` with `open_context()`. Retrieve only relevant tasks, decisions, lessons and command history.

Backend selection is transparent inside `.codex/context-api`; do not choose MariaDB, SQLite or file storage manually.
