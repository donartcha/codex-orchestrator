# Summarization Policy

Summaries must preserve decisions, constraints, current tasks, known failures and next steps while removing repetition and transient noise.

Use `.codex/context-api` through `open_context()` for persisted summaries and related memory.
